# 排除版本控制
.git
.gitignore

# 排除node_modules（会在容器中重新安装）
node_modules

# 排除构建输出
dist
dist-ssr

# 排除开发工具配置
.vscode
.idea
*.swp
*.swo

# 排除日志文件
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# 排除OS生成的文件
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 排除环境变量文件（稍后会通过环境变量传递）
.env*
!.env.example
!.env.local

# 排除测试覆盖率
coverage

# 排除Docker相关文件
Dockerfile*
docker-compose*
.dockerignore

# 排除文档
*.md
README*

# 排除临时文件
*.tmp
*.temp 